; What collapses. A declaration folds to its signature, which is the whole
; point of signature sufficiency: the line that remains is the one a reader
; needs.

[
  (struct_body)
  (block)
  (struct_literal)
  (parameters)
  (arguments)
] @fold
