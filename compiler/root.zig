//! What a front end needs from the compiler. Everything else is an implementation detail
//! of the pipeline `Compilation` runs.

pub const Compilation = @import("Compilation.zig");
pub const Diagnostic = @import("Diagnostic.zig");
