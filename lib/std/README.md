# std

The standard library. A file here is reached as `std.<name>`, and a `pub`
declaration inside it is reached as `std.<name>.<decl>`.

It is empty. The language has no strings, no slices, and no way to allocate,
so there is nothing yet that could be written here.
