use std.text

struct Node {
    plain: i64
    read_only: *Node
    writable: *var Node
    maybe: ?Node
    maybe_pointer: ?*var Node
    qualified: text.Buffer
    deep: ?*var mem.Buffer
}

fn signatures(a: *Node, b: *var Node, c: ?i64) !*var Node {
    return b
}

fn returns_nothing(a: i64) {}
