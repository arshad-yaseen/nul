fn in_an_if(n: i64) i64 {
    if n {
        return 1
    }
    return 0
}

fn in_a_while(n: i64) i64 {
    while n {
        return 1
    }
    return 0
}

fn on_an_optional(maybe: ?i64) i64 {
    if maybe {
        return 1
    }
    return 0
}
