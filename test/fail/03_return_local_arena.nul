use std.mem.Arena

let Session = struct {
    id: i64
}

/// The arena is created inside the function, so it dies before the caller can
/// look at what it returned.
fn open() *Session {
    var local = Arena.init()

    var s = local.create(Session)
    s.id = 7

    return s
}
