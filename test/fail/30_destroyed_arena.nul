use std.mem.Arena

/// 'reset' empties an arena that stays alive. 'destroy' is final, so nothing can
/// come from the arena afterward.
fn revive(arena: Arena) {
    var scratch = arena.child()

    var first = scratch.create(i64)
    scratch.destroy()

    var second = scratch.create(i64)
}
