type true
type false
type bool = true | false

fn returning() i64 {
    defer return 1
    return 0
}

fn breaking(n: i64) i64 {
    if n > 0 {
        defer break
        return 1
    }
    return 0
}
