type true
type false
type bool = true | false

fn bare_numbers(c: bool) i64 {
    let n = if c { 10 } else { 100 }
    return n
}

fn nothing_at_all(c: bool) i64 {
    let n = if c { effect() } else { effect() }
    _ = n
    return 0
}

fn effect() {}
