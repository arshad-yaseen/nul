use std.mem.Arena

let Box = struct {
    arena: Arena
    item:  *i64
}

/// Allocating from an arena reached through a pointer is safe. Releasing one is
/// not, because the proof compares names and this function has none.
fn clear(b: *var Box) {
    b.arena.reset()
}
