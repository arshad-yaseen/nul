use std.mem.Arena

let Reading = struct {
    value: i64
}

let Cache = struct {
    latest: *Reading
}

/// A borrowed parameter may be read and mutated, but it may not be kept.
fn remember(arena: Arena, sample: *Reading) *Cache {
    var c = arena.create(Cache)

    c.latest = sample

    return c
}
