error[E0238]: 'reset' needs the arena's name, and this is a path to one
 --> test/fail/release_through_path.nul:9:5
  |
9 |     list.arena.reset()
  |     ^^^^^^^^^^ not a name
  |
  = help: every value in the arena dies at this instant, and only the function that created the arena can see them; release it there, by name

