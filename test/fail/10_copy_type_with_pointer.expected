error[E0016]: 'Pair' holds a pointer, so it cannot be copied

   ┌─ test/fail/10_copy_type_with_pointer.nul:15:23
   │
15 │     return arena.copy(p)
   │                       ^  this is 'Pair'
   │
   = note: 'left' is '*Inner', and a copy moves the value rather than what it points at,
           so the copy would still reach into the arena the original came from.
   │
   = help: copy what the pointer reaches, and rebuild around it
   │
