use std.mem.Arena

fn churn(arena: Arena, rounds: i64) {
    var scratch = arena.child()
    var round: i64 = 0
    while round < rounds {
        var slot = scratch.create[i64]()
        _ = slot
        scratch.destroy()
        round = round + 1
    }
}
