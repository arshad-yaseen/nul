fn after_return(a: i64) i64 {
    return 1
    let ignored = a + 1
    return ignored
}

fn after_every_arm_exits(c: bool) i64 {
    if c {
        return 1
    } else {
        return 2
    }
    let ignored = 3
    return ignored
}
