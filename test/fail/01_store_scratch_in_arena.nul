use std.mem.Arena

let Box = struct {
    item: *i64
}

/// The box outlives the scratch arena, so it cannot hold a pointer into it.
fn leak(arena: Arena) *Box {
    var scratch = arena.child()

    var box = arena.create(Box)
    var temp = scratch.create(i64)

    box.item = temp

    return box
}
