fn double(n: i64) i64 {
    return n * 2
}

pub fn main() i64 {
    var total: i64 = 0
    var i: i64 = 0
    while i < 5 {
        total = total + double(i)
        i = i + 1
    }
    return total
}
