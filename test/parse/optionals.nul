fn defaults(opt: ?i64) i64 {
    let a = opt orelse 0
    let b = opt orelse a orelse 0
    return a + b
}

fn capture(opt: ?i64) i64 {
    if opt |v| {
        return v
    } else {
        return 0
    }
}

fn plain(opt: ?i64) {
    if opt |v| {
        _ = v
    }
    let nothing: ?i64 = null
    _ = nothing
}
