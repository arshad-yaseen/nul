use std.mem.Arena

struct Probe {
    weight: i64
}

fn peek(arena: Arena) i64 {
    var work = arena.child()
    var probe = work.create[Probe]()
    probe.weight = 42
    let kept = probe.weight
    work.destroy()
    return kept
}
