// What '_ =' drops. A value goes away on purpose, and the expression is still
// checked and still emitted.

fn quiet() i64 {
    return 5
}

fn drive() {
    _ = quiet()
    _ = 5 + 5
    let bound = quiet()
    _ = bound
}
