use std.mem.Arena

let Box = struct {
    arena: Arena
    item:  *i64
}

/// An arena stored in a struct outlives that struct, by the invariant, so what it
/// allocates can always be stored back into the struct. Nothing to check.
fn refill(b: *var Box) {
    b.item = b.arena.create(i64)
}
