use std.mem.Arena

struct Work {
    weight: i64
}

fn measure(arena: Arena, deep: bool) i64 {
    if deep {
        var scratch = arena.child()
        var probe = scratch.create[Work]()
        probe.weight = 40
        return probe.weight + 2
    }
    return 0
}
