struct Inner {
    hits: i64
}

struct Outer {
    inner: Inner
    peer: ?*var Outer
}

fn bump(counter: *var Inner, by: i64) {
    counter.hits = counter.hits + by
}

fn deep(out: *var Outer) {
    out.inner.hits = out.inner.hits + 1
    if out.peer |other| {
        other.inner.hits = 0
    }
}
