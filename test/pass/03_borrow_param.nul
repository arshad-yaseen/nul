use std.mem.Arena

let Counter = struct {
    hits: i64
}

/// A borrowed parameter cannot outlive the call that created it, so reading and
/// mutating one needs no thought and no arena. '*var' says it is written to.
fn bump(c: *var Counter, by: i64) {
    c.hits = c.hits + by
}
