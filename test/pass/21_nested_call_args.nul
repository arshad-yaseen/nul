fn add(a: i64, b: i64) i64 {
    return a + b
}

/// An argument that is itself a call builds an argument list of its own while the
/// outer list is still growing, so the lists have to commit in one piece each.
fn total(x: i64) i64 {
    return add(add(x, 1), add(add(2, 3), x))
}
