use std.mem.Arena

let Node = struct {
    value: i64
    next:  *Node
}

/// Handing back part of a borrowed structure is safe. The caller already owns
/// everything reachable from what it lent, so the result cannot outlive it.
fn tail(head: *Node) *Node {
    return head.next
}
